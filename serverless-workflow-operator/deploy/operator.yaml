apiVersion: apps/v1
kind: Deployment
metadata:
  name: serverless-workflow-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      name: serverless-workflow-operator
  template:
    metadata:
      labels:
        name: serverless-workflow-operator
    spec:
      serviceAccountName: serverless-workflow-operator
      containers:
        - name: serverless-workflow-operator
          image: quay.io/xiezhang7/serverless-workflow-operator:0.1
          command:
          - serverless-workflow-operator
          imagePullPolicy: Always
          env:
            - name: WATCH_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: OPERATOR_NAME
              value: "serverless-workflow-operator"
